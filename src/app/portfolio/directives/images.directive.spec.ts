import { ImagesDirective } from './images.directive';

describe('ImagesDirective', () => {
  it('should create an instance', () => {
    const directive = new ImagesDirective();
    expect(directive).toBeTruthy();
  });
});
